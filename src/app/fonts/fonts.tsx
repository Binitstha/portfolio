import { Sora } from "next/font/google";
export const sora = Sora({
  weight: "400",
  subsets: ["latin"],
});

export const soraBold = Sora({
  weight: "800",
  subsets: ["latin"],
});

export const soraLightBold = Sora({ weight: "600", subsets: ["latin"] });
